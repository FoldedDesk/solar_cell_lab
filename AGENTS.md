# AGENTS 索引与规范

## 目的
- 本文件仅用于索引与协作规范。
- 详细需求、实现约束、验收标准只以 `prs/` 目录内对应 PR 文档为准。

## 强制规则
- 启用 RTK（作为默认执行规则）。
- 启用 PR 编号机制，编号从 `001` 开始递增，不可复用。

## 目录与命名约定
- 所有 PR 文档统一放在：`prs/`
- 命名格式：`PR-<三位编号>-<主题>.md`
- 示例：
  - `PR-001-4k-resolution-adaptation.md`
  - `PR-002-xxx.md`

## 执行规则
- 开发与验证优先读取 `prs/` 内 PR 文档。
- 若本文件与 `prs/` 内 PR 文档存在冲突，以 `prs/` 内文档为准。
